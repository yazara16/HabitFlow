import { Component } from '@angular/core';

@Component({
  selector: 'app-calendar',
  standalone: true,
  template: `
    <h2 class="text-2xl font-bold mb-6">Calendario</h2>
    <p class="text-muted-foreground">Vista de calendario en construcción.</p>
  `
})
export class CalendarComponent {}
